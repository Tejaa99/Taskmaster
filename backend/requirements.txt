# Core dependencies
flask==2.3.3
flask-cors==4.0.0
flask-pymongo==2.3.0
python-dotenv==1.0.0
bcrypt==4.0.1
pyjwt==2.8.0
pymongo==4.5.0
email-validator==2.1.0
werkzeug==2.3.7

# Email
flask-mail==0.9.1
apscheduler==3.10.4

# Production server
gunicorn==21.2.0

# Remove pandas and reportlab for now
# We'll add them back after fixing Python version